{
    "$schema": "../../validation-test-schema.json",
    "description": "optional float-overflow validation",
    "comment": "Optional because it requires handling of floating-point values that overflow typical number representations.",
    "tests": [
        {
            "description": "all integers are multiples of 0.5, if overflow is handled",
            "compatibility": "4",
            "schema": {
                "type": "number", 
                "multipleOf": 0.5
            },
            "tests": [
                {
                    "description": "valid if optional overflow handling is implemented",
                    "data": 1e308,
                    "valid": true
                }
            ]
        }
    ]
}
