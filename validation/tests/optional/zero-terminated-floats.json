{
    "$schema": "../../validation-test-schema.json",
    "description": "optional zeroTerminatedFloats validation",
    "comment": "Optional because some languages do not distinguish between different types of numeric value, so they cannot correctly treat a float such as 1.0 as not being an integer.",
    "tests": [
        {
            "description": "some languages do not distinguish between different types of numeric value",
            "compatibility": "3,<=4",
            "schema": {
                "type": "integer"
            },
            "tests": [
                {
                    "description": "a float is not an integer even without fractional part",
                    "data": 1.0,
                    "valid": false
                }
            ]
        }
    ]
}
